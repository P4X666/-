/**
 * 
 */
(function (root) {
    root.Carousel = Carousel()
    function Carousel() {
        
        function init({target,height=200,interval=4000,initial_index=0}) {
            
        }
        function render(imgList=[]) {
            
        }
        return {
            init,
            render
        }
    }
})(window)